package view.panels;

import javax.swing.*;
import java.awt.*;

public class RightChatPanel extends JPanel {
    public RightChatPanel(){
        setBackground(new Color(45,45,45));
        setLayout(new BorderLayout());
    }
}
