package view.panels;

import javax.swing.*;
import java.awt.*;

public class RightPanel extends JPanel {
    public RightPanel(){
        setBackground(new Color(45,45,45));
        setLayout(new BorderLayout());
    }
}
